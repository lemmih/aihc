extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  cast :: (b ~ c, a ~ b) => a -> b -> c
  cast x _ = x
  result = cast On Off :: Flag
expression: result
output: |
  On
status: pass
reason: Two given equalities form a transitive proof.

evaluators:
- grin
