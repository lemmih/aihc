extensions:
- GADTs
- TypeOperators
- FlexibleInstances
modules:
- |
  module Test where
  import GHC.Types (Char)
  data DL a = DL [a]
  fromL :: [a] -> DL a
  fromL = DL
  class IsStr a where
    fromStr :: [Char] -> a
  instance (a ~ Char) => IsStr (DL a) where
    fromStr = fromL
expression: 'fromStr "ok" :: DL Char'
output: |
  DL (: 'o' (: 'k' []))
evaluators:
- grin
status: pass
reason: An instance equality supplies evidence for the method argument and result.
