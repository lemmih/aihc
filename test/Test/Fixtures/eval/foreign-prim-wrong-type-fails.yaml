extensions:
  - MagicHash
modules:
  - |
    module Test where
    foreign import prim (+#) :: Int# -> Int#
output: ""
expression: |
  (+#) 1#
error: 'typecheck error: Test: foreign import prim +# must repeat the type of GHC.Prim.+#: expected Int# → Int# → Int#, got Int# → Int#'
status: fail
reason: known foreign import prim declarations with incorrect types are rejected before evaluation
