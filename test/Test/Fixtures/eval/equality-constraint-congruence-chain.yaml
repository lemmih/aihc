extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  cast :: (a ~ b, b ~ a, [b] ~ c) => [a] -> c
  cast x = x
  result = case cast [On] :: [Flag] of
    [x] -> x
    _ -> Off
status: pass
reason: Congruence composes with given equalities and terminates with a cycle.
expression: result
output: |
  On
evaluators:
- grin
