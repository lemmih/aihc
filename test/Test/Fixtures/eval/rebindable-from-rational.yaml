extensions:
  - RebindableSyntax
modules:
  - |
    {-# LANGUAGE RebindableSyntax #-}
    module Test where

    import Prelude (Bool (..))

    fromRational _ = True

    half :: Bool
    half = 0.5
output: |
  True
expression: |
  half
status: pass
reason: RebindableSyntax gives a fractional literal the in-scope fromRational
