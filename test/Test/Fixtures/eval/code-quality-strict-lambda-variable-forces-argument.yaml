extensions:
  - BangPatterns
  - MagicHash
modules:
  - |
    module Test where

    import GHC.Prim (raise#)

    data Failure = Failure

    result :: ()
    result = (\ !value -> ()) (raise# Failure)
output: ""
exception: Failure
expression: result
status: pass
reason: A strict variable pattern must force the lambda argument.
