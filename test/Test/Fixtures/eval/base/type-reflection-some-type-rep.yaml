modules:
  - |
    module Test where

    import Type.Reflection (SomeTypeRep (..), TypeRep, typeRep, eqTypeRep, rnfSomeTypeRep)

    boolRep :: TypeRep Bool
    boolRep = typeRep

    someBool :: SomeTypeRep
    someBool = SomeTypeRep boolRep

    result :: Bool
    result =
      case rnfSomeTypeRep someBool of
        () ->
          case someBool of
            SomeTypeRep representation ->
              case eqTypeRep representation boolRep of
                Just _ -> True
                Nothing -> False
output: |
  True
expression: result
status: pass
reason: Type.Reflection SomeTypeRep wraps TypeRep a like GHC
