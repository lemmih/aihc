extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  cast :: (a ~ b) => [a] -> [b]
  cast xs = xs
  result = case cast [On] :: [Flag] of
    [x] -> x
    _ -> Off
expression: result
output: |
  On
status: pass
reason: Equality lifts through a list type constructor. AIHC still lacks complete equality support.

evaluators:
- grin
