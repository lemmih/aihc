name: Cross Compile apple-arm64

# Compile the examples for apple-arm64 on an amd64 Linux host without linking,
# then link and run them on an arm64 macOS host. A failure opens a pull request
# holding the report.

on:
  schedule:
    - cron: "43 4 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

env:
  TARGET: apple-arm64

jobs:
  compile:
    name: Compile examples on linux-amd64
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Nix CI
        uses: ./.github/actions/setup-nix-ci
        with:
          r2-account-id: ${{ vars.R2_ACCOUNT_ID }}
          r2-access-key: ${{ vars.R2_ACCESS_KEY }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          nix-cache-private-key: ${{ secrets.NIX_CACHE_PRIVATE_KEY }}

      - name: Cross-compile the examples
        run: |
          set -o pipefail
          nix build ".#cross-examples-$TARGET" --print-build-logs --out-link cross-examples 2>&1 | tee compile.log

      - name: Summarize compilation
        run: |
          {
            echo "### Compilation for $TARGET"
            echo
            for bundle in cross-examples/*/; do
              echo "- $(basename "$bundle"): $(<"$bundle/status")"
            done
          } >>"$GITHUB_STEP_SUMMARY"

      - name: Pack the bundles
        # tar keeps the hard links that make identical archives share one copy.
        run: tar -C cross-examples -cf cross-examples.tar .

      - name: Upload the bundles
        uses: actions/upload-artifact@v7
        with:
          name: cross-examples-${{ env.TARGET }}
          path: cross-examples.tar
          retention-days: 7
          if-no-files-found: error

      - name: Upload the compile log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: compile-log-${{ env.TARGET }}
          path: compile.log
          retention-days: 7
          if-no-files-found: ignore

  link-and-run:
    name: Link and run on apple-arm64
    needs: compile
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download the bundles
        uses: actions/download-artifact@v8
        with:
          name: cross-examples-${{ env.TARGET }}

      - name: Unpack the bundles
        run: |
          mkdir cross-examples
          tar -C cross-examples -xf cross-examples.tar

      - name: Link and run the examples
        run: scripts/link-and-run-example-bundles.sh cross-examples results

      - name: Upload the results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: results-${{ env.TARGET }}
          path: results
          retention-days: 7
          if-no-files-found: ignore

  report:
    name: Open a failure report
    needs:
      - compile
      - link-and-run
    if: always() && (needs.compile.result != 'success' || needs.link-and-run.result != 'success')
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download the artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Write the report
        run: |
          if [[ -f "artifacts/cross-examples-$TARGET/cross-examples.tar" ]]; then
            mkdir cross-examples
            tar -C cross-examples -xf "artifacts/cross-examples-$TARGET/cross-examples.tar"
          fi
          mkdir -p docs/cross-compile-reports
          scripts/cross-compile-report.sh \
            "docs/cross-compile-reports/$TARGET.md" \
            "$TARGET" \
            cross-examples \
            "artifacts/results-$TARGET" \
            "artifacts/compile-log-$TARGET/compile.log"
          cat "docs/cross-compile-reports/$TARGET.md" >>"$GITHUB_STEP_SUMMARY"

      - name: Open pull request with the report
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.AUTOMATION_PR_TOKEN }}
          branch-token: ${{ secrets.AUTOMATION_PR_TOKEN }}
          commit-message: "ci: report ${{ env.TARGET }} cross-compilation failure"
          add-paths: docs/cross-compile-reports/${{ env.TARGET }}.md
          branch: "automation/cross-compile-${{ env.TARGET }}-failure"
          title: "ci: report ${{ env.TARGET }} cross-compilation failure"
          body: |
            This PR was created automatically by the weekly cross-compilation workflow.

            The examples were compiled for ${{ env.TARGET }} on linux-amd64 and linked and run on ${{ env.TARGET }}. At least one step failed; the report lists every example and the logs of the failures.

            Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
