module Text.Read
  ( Read (..),
    ReadS,
    read,
    reads,
    readParen,
    Lexeme (..),
    lexP,
    parens,
    readListDefault,
    readListPrecDefault,
    ReadPrec,
    Prec,
    minPrec,
    prec,
    step,
    reset,
    get,
    look,
    (+++),
    (<++),
    pfail,
    choice,
    readPrec_to_S,
    readS_to_Prec,
  )
where

import GHC.Read
  ( Read (..),
    ReadS,
    lexP,
    parens,
    readListDefault,
    readListPrecDefault,
    readParen,
  )
import GHC.Read.Lex (Lexeme (..))
import Text.ParserCombinators.ReadPrec
  ( Prec,
    ReadPrec,
    choice,
    get,
    look,
    minPrec,
    pfail,
    prec,
    readPrec_to_S,
    readS_to_Prec,
    reset,
    step,
    (+++),
    (<++),
  )
import Prelude (read, reads)
