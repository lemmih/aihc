module Data.String
  ( String,
    IsString (..),
    lines,
    words,
    unlines,
    unwords,
  )
where

import GHC.Prim.String (IsString (..))
import Prelude (String, lines, unlines, unwords, words)
