annotated:
- |-
  module Test where
  import GHC.Types (List (..))
  (++) :: [a] -> [a] -> [a]
  (++) [] ys = ys
  │    │  └─ type: [a]
  │    └─ type: [a]
  └─ type: ∀ a. [a] → [a] → [a]
  (++) (x : xs) ys = x : (xs ++ ys)
  │     │ │ │   │      │     └─ type: [a] → [a] → [a]; type-args: a
  │     │ │ │   │      └─ type: a → [a] → [a]; type-args: a
  │     │ │ │   └─ type: [a]
  │     │ │ └─ type: [a]
  │     │ └─ type: [a]
  │     └─ type: a
  └─ type: ∀ a. [a] → [a] → [a]
extensions: []
modules:
- |
  module Test where
  import GHC.Types (List (..))
  (++) :: [a] -> [a] -> [a]
  (++) [] ys = ys
  (++) (x : xs) ys = x : (xs ++ ys)
reason: list syntax uses the wired list type constructor of GHC.Types
status: pass
