extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  import qualified GHC.Types as T
  data Flag = Off | On
  cast :: (a T.~ b) => a -> b
  cast x = x
  result = cast On :: Flag
status: xfail
reason: A qualified equality class has the same meaning as equality syntax. AIHC still lacks complete equality support.
