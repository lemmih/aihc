annotated:
- |-
  module Test where
  import GHC.Types (Bool (..))
  f x | x == x = x
  │ │     └─ type: a → a → GHC.Types.Bool; type-args: a, a
  │ └─ type: a
  └─ type: ∀ a. a → a
  (==) a b = True
  │    │ └─ type: b
  │    └─ type: a
  └─ type: ∀ a b. a → b → GHC.Types.Bool
extensions: []
modules:
- |
  module Test where
  import GHC.Types (Bool (..))
  f x | x == x = x
  (==) a b = True
reason: a guard expression calling a locally defined operator instantiates it at the argument type
status: pass
