extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  keep :: (a ~ a) => a -> a
  keep x = x
  result = keep On
status: xfail
reason: Reflexive equality needs no external evidence. AIHC cannot resolve the equality class.
