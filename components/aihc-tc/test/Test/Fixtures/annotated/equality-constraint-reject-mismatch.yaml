extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  witness :: (Flag ~ ()) => Flag
  witness = On
  result = witness
status: pass
reason: Equality cannot equate distinct data types.

annotated:
- |
  module Test where
  data Flag = Off | On
       │      │     └─ type: Flag
       │      └─ type: Flag
       └─ type: Type
  witness :: (Flag ~ ()) => Flag
  witness = On
  └─ type: (Flag ~ ()) ⇒ Flag
  result = witness
           └─ error: couldn't match Flag with ()
