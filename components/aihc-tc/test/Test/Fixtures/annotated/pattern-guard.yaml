annotated:
- |-
  module Test where
  import GHC.Types (Bool (..))
  f x | True = x
  │ └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  import GHC.Types (Bool (..))
  f x | True = x
reason: a boolean guard leaves the guarded equation's argument fully polymorphic
status: pass
