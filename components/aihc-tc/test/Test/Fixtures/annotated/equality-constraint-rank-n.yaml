extensions:
- GADTs
- TypeOperators
- RankNTypes
modules:
- |
  module Test where
  data Flag = Off | On
  use :: (forall a b. (a ~ b) => a -> b) -> Flag
  use f = f On
  cast :: (a ~ b) => a -> b
  cast x = x
  result = use cast
status: pass
reason: Equality evidence remains valid through type checks.
