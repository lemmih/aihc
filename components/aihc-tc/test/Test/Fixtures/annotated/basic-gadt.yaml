annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: Type
  data GADT a where
       └─ type: Type → Type
    IsBool :: GADT Bool
    └─ type: ∀ a. (a ~ Bool) ⇒ GADT a
  not True = False
  │   └─ type: Bool
  └─ type: Bool → Bool
  not False = True
  │   └─ type: Bool
  └─ type: Bool → Bool

  fn :: a -> GADT a -> a
  fn x IsBool = not x
  │  │ └─ type: GADT a; type-args: a; evidence: given a ~ Bool
  │  └─ type: a
  └─ type: ∀ a. a → GADT a → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  data GADT a where
    IsBool :: GADT Bool
  not True = False
  not False = True

  fn :: a -> GADT a -> a
  fn x IsBool = not x
status: pass
