extensions:
- GADTs
- TypeOperators
- DataKinds
- PolyKinds
modules:
- |
  module Test where
  data Flag = Off | On
  data Proxy (a :: k) = Proxy
  bad :: (Flag ~ 'On) => Proxy Flag -> Proxy 'On
  bad x = x
typecheck: false
status: xfail
reason: Homogeneous equality requires both arguments to have the same kind. AIHC cannot resolve the
  equality class.
