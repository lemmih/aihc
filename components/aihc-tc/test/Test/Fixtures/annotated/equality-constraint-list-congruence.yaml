extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  cast :: (a ~ b) => [a] -> [b]
  cast xs = xs
  result = case cast [On] :: [Flag] of
    [x] -> x
    _ -> Off
status: pass
reason: Equality lifts through a list type constructor. AIHC cannot resolve the equality class.
