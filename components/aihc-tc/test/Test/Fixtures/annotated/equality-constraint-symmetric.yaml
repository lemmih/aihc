extensions:
- GADTs
- TypeOperators
modules:
- |
  module Test where
  data Flag = Off | On
  cast :: (b ~ a) => a -> b
  cast x = x
  result = cast On :: Flag
status: pass
reason: A given equality works in the reverse direction.
