extensions:
- GADTs
- TypeOperators
- FlexibleInstances
modules:
- |
  module Test where
  data Flag = Off | On
  data DL a = DL a
  wrap :: Flag -> DL Flag
  wrap = DL
  class FromFlag a where
    fromFlag :: Flag -> a
  instance (a ~ Flag) => FromFlag (DL a) where
    fromFlag = wrap
status: xfail
reason: An instance-context equality lifts through a type constructor in a method result. AIHC still lacks complete equality support.
