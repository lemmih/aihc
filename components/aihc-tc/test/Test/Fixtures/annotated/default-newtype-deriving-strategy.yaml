annotated:
- |-
  {-# LANGUAGE GeneralizedNewtypeDeriving #-}
  module Main where
  class C a where
  └─ class methods: c ∷ ∀ a. (C a) ⇒ a → a
    c :: a -> a
  newtype N a = N a
  │       │     └─ type: ∀ a. a → N a
  │       └─ type: Type → Type
  └─ deriving plans: newtype C (N a) [context: C a] [methods: c]
    deriving C
             └─ $fCNa ∷ ∀ a. (C a) ⇒ C (N a)
             └─ type: a → a; type-args: a; evidence: given C a
             └─ c ∷ N a → N a
             └─ type: a → N a; type-args: a
             └─ type: N a
             └─ type: a
             └─ type: N a
             └─ type: a
             └─ type: N a → N a
extensions:
- GeneralizedNewtypeDeriving
modules:
- |
  {-# LANGUAGE GeneralizedNewtypeDeriving #-}
  module Main where
  class C a where
    c :: a -> a
  newtype N a = N a
    deriving C
reason: default deriving selects newtype when only GeneralizedNewtypeDeriving applies
status: pass
