extensions:
- GADTs
- TypeOperators
- MultiParamTypeClasses
- ExplicitNamespaces
modules:
- |
  module GHC.Types where
  class a ~ b
  infix 4 ~
- |
  module Bridge (type (~)) where
  import GHC.Types (type (~))
- |
  module Main where
  import Bridge (type (~))
  cast :: (a ~ b) => a -> b
  cast x = x
annotated:
- |
  module Bridge (type (~)) where
  import GHC.Types (type (~))
- |
  module GHC.Types where
  class a ~ b
          └─ t GHC.Types
  infix 4 ~
- |
  module Main where
  import Bridge (type (~))
  cast :: (a ~ b) => a -> b
  └─ v Main  └─ t GHC.Types
  cast x = x
  │    │   └─ v 0
  │    └─ v 0
  └─ v Main
status: pass
reason: An equality class re-export preserves its GHC.Types identity.
