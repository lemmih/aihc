extensions: [UnboxedSums]
modules:
- |
  module Main where
  data T = T
  (# x | #) = (# T | #)
  y = x
annotated:
- |-
  module Main where
  data T = T
       │   └─ v Main
       └─ t Main
  (# x | #) = (# T | #)
     │           └─ v Main
     └─ v Main
  y = x
  │   └─ v Main
  └─ v Main
status: xfail
reason: Module exports omit variables inside unboxed sum patterns.
