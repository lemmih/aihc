extensions:
- GADTs
- TypeOperators
- FlexibleInstances
modules:
- |
  module Test where
  import GHC.Types (Char)
  data DL a = DL [a]
  fromL :: [a] -> DL a
  fromL = DL
  class IsStr a where
    fromStr :: [Char] -> a
  instance (a ~ Char) => IsStr (DL a) where
    fromStr xs = fromL xs
status: pass
reason: An instance equality supplies evidence for the method argument and result.
