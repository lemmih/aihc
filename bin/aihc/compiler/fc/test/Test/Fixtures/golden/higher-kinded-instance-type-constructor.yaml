extensions:
  - KindSignatures
modules:
  - |
    module Test where
    import GHC.Types (Type)
    data Box a = Box a
    class Map (f :: Type -> Type) where
      mapValue :: f a -> f a
    instance Map Box where
      mapValue value = value
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.tType;
    2.tLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.tType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.tType;
    2.tType :: 2.tType;
    2.vBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.vLifted :: 2.tLevity

  import synonyms
    2.tLiftedRep = 2.vBoxedRep 2.vLifted;
    2.tType = 2.tTYPE 2.tLiftedRep

  pub type 1.tBox (a : 2.tType) :: 2.tType {
      pub 1.vBox :: ∀(a : 2.tType). a 2.→ 1.tBox a
  }

  pub type 1.t$Dict$Map (f : 2.tType 2.→ 2.tType) :: 2.tType {
      pub 1.v$Dict$Map :: ∀(f : 2.tType 2.→ 2.tType). (∀(a : 2.tType). f a 2.→ f a) 2.→ 1.t$Dict$Map f
  }

  pub val 1.vmapValue :: ∀(f : 2.tType 2.→ 2.tType) (a : 2.tType). 1.t$Dict$Map f 2.→ f a 2.→ f a
   = Λ(f : 2.tType 2.→ 2.tType).
    Λ(a : 2.tType).
      λ($d0 : 1.t$Dict$Map f).
        case $d0 as ($dict : 1.t$Dict$Map f) return (f a 2.→ f a) of {
            1.v$Dict$Map ($method0 : ∀(a{1} : 2.tType). f a{1} 2.→ f a{1}) →
                $method0 @a
        }

  pub val 1.v$fMapBox :: 1.t$Dict$Map 1.tBox
   = 1.v$Dict$Map @1.tBox (Λ(a : 2.tType).
    λ(x : 1.tBox a).
      x)
status: pass
reason: ''
