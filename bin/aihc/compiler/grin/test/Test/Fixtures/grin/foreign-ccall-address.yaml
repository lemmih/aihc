expected: |-
  scope 1 = "" Test

  constructor 1.Ptr [AddrRep]

  foreign putsAddress = address "puts" :: () -> addr ! pure

  global 1.address = (F$address_thunk)

  $address_thunk -> BoxedRep Lifted =
    foreign_result :: AddrRep <-
      foreign-call putsAddress = address "puts" :: () -> addr ! pure with
    store (C1.Ptr (foreign_result :: AddrRep))
extensions:
- ForeignFunctionInterface
- MagicHash
source: |
  module Test where
  import GHC.Prim
  data Ptr a = Ptr Addr#
  foreign import ccall "&puts" putsAddress :: Ptr ()

  address :: Ptr ()
  address = putsAddress
reason: takes the address of a static C symbol instead of calling it
status: pass
