expected: |-
  scope 1 = "" Target

  primitive +# :: IntRep/2

  global 1.visible = (P$visible/[[IntRep]])

  $helper (x :: IntRep) -> IntRep =
    primitive-call @IntRep +# (x :: IntRep) (1 :: IntRep)

  $twice (x :: BoxedRep Lifted) (y :: IntRep) -> IntRep =
    function_whnf :: BoxedRep Lifted <-
      eval @(BoxedRep Lifted) (x :: BoxedRep Lifted)
    argument :: IntRep <-
      apply @IntRep (function_whnf :: BoxedRep Lifted) (y :: IntRep)
    function_whnf%1 :: BoxedRep Lifted <-
      eval @(BoxedRep Lifted) (x :: BoxedRep Lifted)
    apply @IntRep (function_whnf%1 :: BoxedRep Lifted) (argument :: IntRep)

  $visible (x :: IntRep) -> IntRep =
    argument :: BoxedRep Lifted <-
      store (P$helper/[[IntRep]])
    call @IntRep $twice (argument :: BoxedRep Lifted) (x :: IntRep)
extensions:
- MagicHash
modules:
- |
  module Target (visible) where
  import GHC.Prim
  helper :: Int# -> Int#
  helper x = x +# 1#
  twice :: (Int# -> Int#) -> Int# -> Int#
  twice f x = f (f x)
  visible :: Int# -> Int#
  visible x = twice helper x
reason: passing an unexported function builds its partial application in place
status: pass
