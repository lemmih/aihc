expected: |-
  scope 1 = "" Test

  constructor 1.Address [AddrRep]

  primitive nullAddr# :: AddrRep/0

  global 1.Address = (C1.Address/1)

  global 1.value = (F$value_thunk)

  $value_thunk -> BoxedRep Lifted =
    argument :: AddrRep <-
      primitive-call @AddrRep nullAddr#
    store (C1.Address (argument :: AddrRep))
extensions:
- MagicHash
reason: evaluates an imported nullary primitive through its global binding
source: |
  module Test where
  import GHC.Prim
  data Address = Address Addr#
  value :: Address
  value = Address nullAddr#
status: pass
