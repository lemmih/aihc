expected: |-
  scope 1 = "" Test

  constructor 1.IS [IntRep]

  global 1.value = (C1.IS (42 :: IntRep))
extensions:
- MagicHash
reason: removes the applied newtype conversion closure and does not allocate a newtype node
source: |
  module Test where
  import GHC.Prim
  data Int = IS Int#
  newtype Identity a = Identity a
  value :: Identity Int
  value = Identity (IS 42#)
status: pass
