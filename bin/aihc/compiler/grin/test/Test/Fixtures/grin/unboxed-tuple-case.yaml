expected: |-
  scope 1 = "" Test

  constructor 1.Box [IntRep]

  global 1.value = (F$value_thunk)

  $value_thunk -> BoxedRep Lifted =
    store (C1.Box (1 :: IntRep))
extensions:
- MagicHash
- UnboxedTuples
reason: returns heterogeneous unboxed-tuple components directly without allocating
  or matching a node
source: |
  module Test where
  import GHC.Prim
  data Box = Box Int#
  value :: Box
  value = Box (case (# 1#, 2## #) of (# first, _ #) -> first)
status: pass
