extensions: []
source: |
  module Test where
  data Result = Result
  class Read a where
    reads :: Result -> a
expected: |-
  scope 1 = "" Test

  constructor 1.Result []

  constructor 1.$Dict$Read [BoxedRep Lifted]

  global 1.reads = (P$reads/1)

  $reads ($d0 :: BoxedRep Lifted) -> BoxedRep Lifted =
    case_value :: BoxedRep Lifted <-
      eval @(BoxedRep Lifted) ($d0 :: BoxedRep Lifted)
    case (case_value :: BoxedRep Lifted) as $dict :: BoxedRep Lifted of
      data 1.$Dict$Read ($method0 :: BoxedRep Lifted) ->
        eval @(BoxedRep Lifted) ($method0 :: BoxedRep Lifted)
status: pass
reason: emits a class method selector as a direct global closure
