expected: |-
  scope 1 = "" Test

  constructor 1.Box [IntRep]

  global 1.Box = (C1.Box/1)

  global 1.value = (F$value_thunk)

  $value_thunk -> BoxedRep Lifted =
    store (C1.Box (42 :: IntRep))
extensions:
- EmptyDataDecls
- MagicHash
- UnboxedTuples
reason: omits zero-width State# components from unboxed-tuple returns and binds
source: |
  module Test where
  import GHC.Prim
  data Box = Box Int#
  foreign import prim realWorld# :: State# RealWorld
  value :: Box
  value = Box (case (# realWorld#, 42# #) of (# _, answer #) -> answer)
status: pass
