expected: |-
  scope 1 = "" Target

  constructor 1.Box [IntRep]

  constructor 1.Unit []

  global 1.Unit = (C1.Unit)

  global 1.wrap = (P$wrap/[[IntRep]])

  global 1.unit = (C1.Unit)

  $wrap (x :: IntRep) -> BoxedRep Lifted =
    store (C1.Box (x :: IntRep))
extensions:
- MagicHash
modules:
- |
  module Target (wrap, unit) where
  import GHC.Prim
  data Box = Box Int#
  data Unit = Unit
  wrap :: Int# -> Box
  wrap n = Box n
  unit :: Unit
  unit = Unit
reason: an unexported constructor that takes fields needs no global; a nullary one keeps its shared cell
status: pass
