expected: |-
  scope 1 = "" Test

  foreign labs = "labs" :: (int) -> int ! pure

  global 1.run = (P$run/[[IntRep]])

  $run (x :: IntRep) -> IntRep =
    foreign-call labs = "labs" :: (int) -> int ! pure with (x :: IntRep)
extensions:
- ForeignFunctionInterface
- MagicHash
source: |
  module Test where
  import GHC.Prim
  foreign import ccall safe "labs" labs :: Int# -> Int#

  run :: Int# -> Int#
  run x = labs x
reason: a safe C call lowers like an unsafe C call
status: pass
