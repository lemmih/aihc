program: |
  $entry -> IntRep =
    case (0 :: IntRep) as choice%1 :: IntRep of
      0 ->
        constant (1 :: WordRep)
      _ ->
        constant (2 :: WordRep)
error: result-layout
status: pass
reason: The lint check must reject a first alternative with the wrong result layout.
