-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/

@package sections
@version 1.2.0



-- | The module documentation follows the header fields. It links to
--   <a>Counter</a> and to <a>Data.Sections</a>.
module Data.Sections

-- | A counter.
newtype Counter
Counter :: Int -> Counter

-- | the current count
[getCounter] :: Counter -> Int

-- | A fresh counter at zero.
newCounter :: Counter

-- | Add one.
increment :: Counter -> Counter

-- | Add two counters.
(|+|) :: Counter -> Counter -> Counter
infixl 6 |+|

-- | A pair of the same thing.
type Pair a = (a, a)

-- | A value with a tag.
data Tagged a
Tagged :: String -> a -> Tagged a

-- | the tag
[tagOf] :: Tagged a -> String

-- | Things that can be counted.
class Countable a

-- | Count the elements.
count :: Countable a => a -> Int

-- | Whether there is nothing to count.
isEmpty :: Countable a => a -> Bool

-- | Kept for compatibility.

-- | <i>Deprecated: use <a>increment</a> instead</i>
legacy :: Int -> Int
instance Countable Counter
instance Eq Counter
instance Eq a => Eq (Tagged a)
instance Ord Counter
instance Show Counter
instance Show a => Show (Tagged a)
